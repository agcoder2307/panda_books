name: CI/CD (Docker Compose -> VM)

on:
  push:
    branches: ["master"]

jobs:
  compose-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Sanity check (repo files)
        run: |
          pwd
          ls -la
          test -f Dockerfile
          test -f docker-compose.yaml
          test -f package.json
          test -f yarn.lock
          echo "OK: required files exist"

      - name: Build & Run via docker compose (test)
        run: |
          docker compose -f docker-compose.yaml up -d --build
          docker compose -f docker-compose.yaml ps
          sleep 5
          curl -fsS http://127.0.0.1:5173/ | head -n 20
          docker compose -f docker-compose.yaml down

  deploy:
    needs: compose-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # /opt permission muammosi bo'lmasligi uchun home ichiga yuboramiz
      - name: Copy project to VM (home folder)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "."
          target: "/home/${{ secrets.SERVER_USER }}/panda_books_frontend"

      - name: Deploy on VM via docker compose
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            set -e
            cd /home/${{ secrets.SERVER_USER }}/panda_books_frontend
            docker compose -f docker-compose.yaml down || true
            docker compose -f docker-compose.yaml up -d --build
            docker compose -f docker-compose.yaml ps
